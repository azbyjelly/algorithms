vetor = [-9, -5, 3, 8, 9, -15, 16, -4, 2]
i = 0
while i < len(vetor):
    if vetor[i] < 0:
        vetor[i] = 0
    i += 1

print(vetor)

#while faz tudo que o for i in range faz, mas o for não faz tudo que o while faz
